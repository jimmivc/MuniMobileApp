// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "go.municipalidad.santaana"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
